- name: Initialize pi
  hosts: all
  become: true
  serial: 1
  roles:
    - stop_keepalived
    - bootstrap
    - updates
    - docker
    - pihole
    - start_keepalived

- name: Install Orbital Sync on primary pihole
  hosts: pihole-1-vm
  become: true
  roles:
    - orbital_sync
